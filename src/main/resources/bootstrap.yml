spring:
  application:
    name: SpringBootApp
  cloud:
    consul:
      enabled: true
      host: localhost
      port: 8500
      discovery:
        serviceName: ${spring.application.name}
        instanceId: ${spring.application.name}:${random.value}